# script used to make git commits
git_commit.sh
# directory where object and executables are built
build
# directory where documentation is build
# doc (this directory ignored by master but not by gh-pages).
#
# temporary directory used for stashing files
new
new.*
# temporary files
junk
junk.*
# editor swap files
*.swp
